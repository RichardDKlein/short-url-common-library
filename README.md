# Short URL User Common Library

## Pre-requisites
* Maven

## Building and installing the project
From the project root folder, use Maven to build and install the project:
```bash
$ mvn clean install
```
